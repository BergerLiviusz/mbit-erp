{
  "name": "@mbit-erp/config",
  "version": "1.0.1a",
  "private": true,
  "description": "Shared ERP package and version configuration",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.10",
    "typescript": "^5.3.3"
  }
}
